\documentclass[12pt, paper=a4, parskip, oneside]{scrreprt}
\usepackage[utf8]{inputenc}
\usepackage{silence}
\usepackage[citations, linkAttributes, pipeTables, relativeReferences, inlineNotes=true, tableAttributes, tableCaptions, smartEllipses, rawAttribute, fencedCode, startNumber=false, fancyLists=true, tightLists=false, underscores=false, superscripts]{markdown}
\usepackage[backend=biber, style=numeric, citetracker=true, style=verbose-note]{biblatex}
\addbibresource{bibliography.bib}

{% if author %}
\author{ {{ author }} }
{% endif %}
{% if title %}
\title{ {{ title }} }
{% endif %}

\newcommand{\myfootcite}[1]{\footcite{#1}}

\markdownSetup{
  renderers = {
    cite = {\myfootcite},
  },
}

\begin{document}

{% if title %}
\maketitle
{% endif %}

{% if tableofcontents %}
\tableofcontents % Generate table of contents
{% endif %}

#INCLUDE_CHAPTERS

\printbibliography

\end{document}
